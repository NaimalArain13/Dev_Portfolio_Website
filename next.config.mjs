import { Images } from 'lucide-react';

/** @type {import('next').NextConfig} */
const nextConfig = {
    module:{
        images:{
            domain:["i.pinimg.com"]
        }

    }
};


export default nextConfig;
